# Optional. Leave unset for local dev: the app uses same-origin /api and Vite proxies to the backend (see vite.config.js).
# Set only when the API is on another host (e.g. production) — use HTTPS if the site is served over HTTPS.
# VITE_API_URL=https://api.example.com
